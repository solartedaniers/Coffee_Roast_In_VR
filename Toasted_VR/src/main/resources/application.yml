server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: Toasted_VR
  config:
    import: optional:file:./.env[.properties],optional:file:./Toasted_VR/.env[.properties]

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/toasted_vr_db?useSSL=false&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: ${DB_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}

  jpa:
    database-platform: ${JPA_DATABASE_PLATFORM:org.hibernate.dialect.MySQLDialect}
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    open-in-view: false

  mail:
    host: ${MAIL_HOST:}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS_ENABLE:true}

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000}
  mail:
    enabled: ${MAIL_ENABLED:false}
    from: ${MAIL_FROM:${MAIL_USERNAME:no-reply@toastedvr.local}}
  verification:
    code-length: ${VERIFICATION_CODE_LENGTH:6}
    code-expiration-minutes: ${VERIFICATION_CODE_EXPIRATION_MINUTES:2}
